# Session du 2026-04-04 - Phase 1 complete + migration stack

## Ce qui a ete fait

### Skills installes
- shadcn/ui (1 skill)
- vercel-labs/next-skills (3 skills : next-best-practices, next-cache-components, next-upgrade)
- vercel-labs/agent-skills (7 skills : react-best-practices, deploy-to-vercel, composition-patterns, etc.)

### shadcn/ui installe et configure
- Init avec Tailwind CSS v3 + CSS variables HSL
- 13 composants ajoutes : button, input, label, select, dialog, badge, card, table, dropdown-menu, sheet, separator, textarea, sonner
- Palette forge/iron integree dans tailwind.config.ts

### Migration NextAuth v4 -> Better Auth
- Desinstalle next-auth, installe better-auth
- Nouveau schema Prisma avec tables Better Auth (User, Session, Account, Verification)
- Config serveur : src/lib/auth.ts (betterAuth + prismaAdapter)
- Config client : src/lib/auth-client.ts (createAuthClient)
- Helper session : src/lib/session.ts
- Route API : src/app/api/auth/[...all]/route.ts
- Toutes les API routes mises a jour (getSession au lieu de getServerSession)
- Seed adapte pour Better Auth (User + Account credential)
- Variables d'env : BETTER_AUTH_SECRET, BETTER_AUTH_URL

### Migration SQLite -> PostgreSQL (Vercel Postgres / Neon)
- Schema Prisma : provider "postgresql", ajout directUrl
- .env adapte pour PostgreSQL
- Auth config : provider "postgresql"

### Phase 1 - Auth & Layout (TERMINEE)
- Middleware : protection routes via getSessionCookie (better-auth/cookies)
- Page login : formulaire email/password avec shadcn/ui, theme forge
- Sidebar : navigation (Dashboard, Materiaux, Nouveau devis, Mes devis, Parametres, Deconnexion)
- Header : titre page + nom utilisateur
- Layout authentifie : src/app/(authenticated)/layout.tsx
- Pages placeholder : dashboard, materials, estimates, settings

### Documentation
- markdown/setup-skills.md — Guide des skills installes
- markdown/setup-shadcn-ui.md — Guide shadcn/ui
- markdown/setup-better-auth.md — Guide Better Auth (mis a jour pour PostgreSQL)
- markdown/setup-vercel-postgres.md — Guide Vercel Postgres / Neon
- CLAUDE.md mis a jour avec la stack actuelle

## Decisions prises
- shadcn/ui au lieu de composants custom (elimine Phase 2.1 du ROADMAP)
- Better Auth au lieu de NextAuth (migration faite)
- Vercel Postgres (Neon) au lieu de Turso (zero compte externe, tout dans Vercel)
- Multi-selection pour ajouter des materiaux aux devis (Phase 3)

## Prochaines etapes
1. Creer repo GitHub + push le code
2. Creer projet Vercel + activer Storage Postgres
3. Configurer variables d'env sur Vercel
4. npx prisma db push + seed sur la DB prod
5. Deployer
6. Puis continuer Phase 2 (CRUD Materiaux)
